import * as assert from 'power-assert';

describe('Hoge', () => {
  it('should be true', () => {
    const taro: string = 'tarou';
    const jiro: string = 'jirou';
    assert(taro !== jiro);
    // expect(true).
  });
});
